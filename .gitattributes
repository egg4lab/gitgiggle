# Large practice videos (Git LFS keeps the main repo clone small)
apps/mysore-tracker/videos/*.mp4 filter=lfs diff=lfs merge=lfs -text

# UNIX shebang scripts (wish/Tcl)
tools/virtuoso/*.tcl text eol=lf
