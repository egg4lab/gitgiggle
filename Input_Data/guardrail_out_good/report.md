# Technology Rule Guardrail Report

**Node:** gpdk045
**Source file:** DiffOpAmp.gds
**Baseline:** baseline_gpdk045.yaml
**Violations found:** 0
**Waived:** 0

## Summary

This run found **no violations**. Every layer/datatype pair used in `DiffOpAmp.gds` matches an entry in the `allowed` list of `baseline_gpdk045.yaml`, and no waivers were needed to reach that clean result. The block is clear for signoff/data handoff with respect to layer legality under this baseline version.

## Findings by Category

No entries were reported in any category. For reference, if violations had appeared, they would map to the following categories and likely causes:

- **Unknown** — a `(layer, dt)` pair not present anywhere in the baseline (not `allowed`, `deprecated`, or `reserved`). This typically indicates a fat-fingered datatype, a layer number transcribed from the wrong node's tech file, or a custom/experimental layer that was never registered in the baseline. The probable intended layer is usually the nearest valid datatype on the same layer number, or the equivalent named layer (e.g., "M1") pulled from the correct node's baseline.
- **Deprecated** — a layer that was legal in a prior technology revision but has since been superseded. This usually means the layout was drawn with an old PDK, copied from a legacy cell, or generated by a tool still targeting an older ruleset. The probable intended layer is the deprecated layer's designated replacement, as documented in the current baseline/tech file release notes.
- **Reserved** — a layer set aside for internal tool or flow use (e.g., markers, DRC/LVS scratch layers, EDA-internal bookkeeping) that should never ship in delivered data. This usually indicates leftover debug/marker shapes, an unstripped tool artifact, or a merge that pulled in scratch geometry. The probable intended fix is to delete the shapes entirely rather than remap them to another layer, since reserved layers have no "legal" equivalent for delivery.

No instances of any category were present in this dataset, so no specific cells or bounding boxes require review.

## Suggested Fixes

No fixes are required. To keep this result stable going forward:

- Re-run this check whenever `DiffOpAmp.gds` is regenerated or `baseline_gpdk045.yaml` is updated, since a baseline revision could reclassify a currently-allowed layer as deprecated or reserved.
- If future runs on this block do surface violations, triage `unknown` first (most likely a real error), then `deprecated` (plan a remap), then `reserved` (plan a deletion) — and route any confirmed intentional exceptions through `waivers.yaml` with owner and reason rather than editing the baseline directly.

## Risk Ranking

| Block | Violations | Categories Present | Risk |
|---|---|---|---|
| DiffOpAmp.gds | 0 | none | **Low** |

No blocks in this run carry elevated risk. DiffOpAmp.gds is ranked **Low** risk based on zero violations across all three categories.
